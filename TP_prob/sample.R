# TP
a <- sample(1:6,1, replace = T)
print(a)
for (i in 1:10){
  print(sample(1:6, size = 5, replace = F))
}

s <- c()

figus <- function(n){
  s <- c()
  c <- 0
  while (c<n) {
    ev <- sample(1:n,1)
    
  }
}
