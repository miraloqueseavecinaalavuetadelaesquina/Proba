# X ~ U(a,b), a<b
# densidad f_X -> dunif(x,a,b) x :[a,b]
# acumulada -> punif(x,a,b)
# simu I -> runif(1,a,b) genera un posible resultado de X
# simu II -> runif(N,a,b) genera N posibles resultados de X
